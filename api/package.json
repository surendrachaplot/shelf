{
  "name": "shelf-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node serve.js",
    "dev": "node --env-file=.env serve.js",
    "migrate": "node --env-file=.env -e \"import('./db.js').then(m=>m.migrate()).then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\"",
    "selftest": "node resolve.js --selftest && node classify.js --selftest && node url.js --selftest && node resolveRoute.js --selftest && node publish.js --selftest && node probe.js --selftest && node page.js --selftest && node search.js --selftest && node enrich/index.js --selftest && SHELF_WEB_BASE=https://shelf.example node page.js --selftest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "pg": "^8.13.1"
  }
}
