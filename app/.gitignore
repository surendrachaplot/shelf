# Generated by `expo prebuild`. Regenerated from app.json and the config
# plugins on every EAS build, so a checked-in copy is a stale copy that
# silently wins over the plugins that were supposed to produce it.
/ios
/android
.expo/
